<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Help Fee</title>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="assets/css/estilos.css">

</head>

<body>
    <div id="app"></div>
    <main>
        <div class="contenedor_todo">
            <div class="caja_trasera">
                <div class="caja_trasera_login">
                    <h3>¿Ya tienes cuenta?</h3>
                    <p>inicia sesión para entrar en la pagina</p>
                    <button id="btn_iniciar_sesion">Iniciar sesión</button>
                </div>
                <div class="caja_trasera_register">
                    <h3>¿Aún no tienes cuenta?</h3>
                    <p>registrate para que puedas inciar sesión</p>
                    <button id="btn_registrarse">Registrar</button>
                </div>
            </div>
            <!--formulario de login y registro-->
            <div class="contenedor_login_register">
                <!--login-->
                <form method="POST" action="/signin" class="formulario_login">
                    <h2>Iniciar sesion</h2>
                    <% if(mensajeRegistro.length > 0) {%>
                        <div class="alert alert-danger">
                            <%= mensajeRegistro%>
                        </div>
                        <% } %>
                            <% if(mensajeLogin.length > 0) {%>
                                <div class="alert alert-danger">
                                    <%= mensajeLogin%>
                                </div>
                                <% } %>
                                    <input type="text" placeholder="Correo" name="correo">
                                    <input type="password" placeholder="Contraseña" name="password">
                                    <button type="submit">Entrar</button>
                </form>
                <!--registro-->

                <form method="POST" class="formulario_register" action="/signup">
                    <h2>Registrarse</h2>
                    <% if(mensajeRegistro.length > 0) {%>
                        <div class="alert alert-danger">
                            <%= mensajeRegistro%>
                        </div>
                        <% } %>
                            <input type="text" placeholder="Nombres completos" name="nombres">
                            <input type="text" placeholder="Correo Electronico" name="correo">
                            <input type="password" placeholder="Contraseña" name="password">
                            <input type="password" placeholder="Confirmar Contraseña" name="passwordconfirmar">
                            <button type="submit">Registrarse</button>
                </form>
            </div>
        </div>

    </main>
    <script src="assets/js/script.js"></script>
</body>

</html>